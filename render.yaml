services:
  - type: web
    name: dashboard
    runtime: python
    buildCommand: pip install flask
    startCommand: python test_render.py
    envVars:
      - key: PYTHON_VERSION
        value: python-3.11.8
