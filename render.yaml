services:
  - type: web
    name: dashboard
    runtime: python
    buildCommand: pip install -r requirements-render.txt
    startCommand: python app_render.py
    envVars:
      - key: PYTHON_VERSION
        value: python-3.11.8
